[build]
  base = "."
  command = "npm run build && echo '=== DIST CONTENTS ===' && ls -la dist/ && ls -la dist/assets/"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
